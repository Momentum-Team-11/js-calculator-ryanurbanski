
FUNCTIONS
document.getElementbyId("name of an element")   // returns the whole tag of that element, can assing to a variable to get other attributes
document.getElementByClassName('class')          // will return an array elemen, not sure when to use this yet

document.queryselector('#display-box-text')         // will return the first matching element
document.queryselectorAll()                         // returns a collection of elements








